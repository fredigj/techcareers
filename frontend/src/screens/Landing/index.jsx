import React from 'react'
import Navbar from '../../components/Navbar'

const Landing = () => {
    // check if it is a seeker or admin
    // if seeker, redirect to dashboard
    // if admin, redirect to admin
  return (
    <div>
      <Navbar/>
      Landing
    </div>
  )
}

export default Landing